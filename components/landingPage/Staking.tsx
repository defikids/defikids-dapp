"use client";

import { Box } from "@chakra-ui/react";

const Staking = () => {
  return (
    <Box
      as="section"
      id="Staking"
      p={4}
      h="50rem"
      bgGradient={["linear(to-b, white, #82add9)"]}
    ></Box>
  );
};

export default Staking;
